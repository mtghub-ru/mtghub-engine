(in-package :mtghub-engine)

(defun initial-setup ()
    "Perform the initial set-up operations to configure your web app's environment for the RSN-MTG library."

    (print "Project is loaded."))

(defun method-for-test (x)
    (match x
        (m (+ m m))))

(initial-setup)
